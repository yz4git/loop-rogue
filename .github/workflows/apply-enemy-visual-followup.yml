name: apply enemy visual followup

on:
  push:
    branches:
      - work/enemy-combat-visual-review-20260830
    paths:
      - .github/workflows/apply-enemy-visual-followup.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/enemy-combat-visual-review-20260830
          persist-credentials: true
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply readability patch
        run: python .github/scripts/apply_enemy_visual_followup.py
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build
      - name: Tests
        run: bash scripts/run-tests.sh
      - name: Lint
        run: npm run lint
      - name: Commit
        run: |
          git config user.name "yz4git"
          git config user.email "65384484+yz4git@users.noreply.github.com"
          git add src tests
          git diff --cached --check
          git commit -m "fix: make enemy warnings readable in close combat"
          git push origin HEAD:work/enemy-combat-visual-review-20260830
